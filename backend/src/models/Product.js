// TODO: Definir model de Produto quando o ORM/banco for configurado
// Exemplo com Sequelize ou similar

/**
 * Product Model
 * 
 * Campos:
 * - id: INTEGER (PK, auto-increment)
 * - name: STRING (NOT NULL)
 * - flavor: STRING
 * - price: DECIMAL (NOT NULL)
 * - oldPrice: DECIMAL
 * - rating: DECIMAL
 * - tag: STRING
 * - category: STRING
 * - image: STRING
 * 
 * Timestamps: createdAt, updatedAt
 */

module.exports = {};
